---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: networkdevices.sonick8s.io
spec:
  group: sonick8s.io
  names:
    kind: NetworkDevice
    listKind: NetworkDeviceList
    plural: networkdevices
    singular: networkdevice
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NetworkDevice is a specification for a NetworkDevice resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkDeviceSpec is the spec for a NetworkDevice resource
            properties:
              os:
                properties:
                  os-type:
                    type: string
                  version:
                    type: string
                type: object
            type: object
          status:
            description: NetworkDeviceStatus is the status for a NetworkDevice resource
            properties:
              conditions:
                items:
                  description: Condition represents a status condition
                  properties:
                    lastProbeTime:
                      format: date-time
                      type: string
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
              lastTransitionTime:
                type: string
              operation:
                description: OperationStatus describes an operation in status
                properties:
                  id:
                    type: string
                  name:
                    type: string
                  operationAction:
                    description: OperationAction describes the action of an operation
                    properties:
                      id:
                        type: string
                      name:
                        type: string
                      status:
                        type: string
                    type: object
                  status:
                    type: string
                type: object
              os:
                properties:
                  os-type:
                    type: string
                  version:
                    type: string
                type: object
              state:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
